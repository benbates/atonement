<%= form_for(@reminder_phrase) do |f| %>
  <% if @reminder_phrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reminder_phrase.errors.count, "error") %> prohibited this reminder_phrase from being saved:</h2>

      <ul>
      <% @reminder_phrase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phrase %><br />
    <%= f.text_field :phrase %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
