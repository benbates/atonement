<%= form_for(@sin_type) do |f| %>
  <% if @sin_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sin_type.errors.count, "error") %> prohibited this sin_type from being saved:</h2>

      <ul>
      <% @sin_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :calories %><br />
    <%= f.number_field :calories %>
  </div>
  <div class="field">
    <%= f.label :phrase %><br />
    <%= f.text_field :phrase %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
